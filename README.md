# qa-backend-karate
Este proyecto implementa una **suite de pruebas automatizadas** para la API pública de **[ServeRest](https://serverest.dev/)**, enfocada en la gestión de usuarios. El objetivo es validar las operaciones CRUD del endpoint `/usuarios` utilizando **Karate DSL**, cubriendo casos positivos, negativos y validaciones de esquemas JSON.
